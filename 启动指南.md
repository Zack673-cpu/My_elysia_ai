# My_elysia_ai 启动指南

## 一键启动（推荐）

前后端用同一个入口启动（`start_app.bat`），推荐直接用桌面快捷方式：

- 快捷方式经 `launch_app.vbs` 隐藏启动，**全程不弹命令行黑框**
- 后端没在跑时，会先静默启动后端（无窗口，日志写到 `%LOCALAPPDATA%\MyElysiaAI\backend.log`）
- 后端已在跑则直接拉起前端
- **关闭窗口退出前端时，后端会同步关闭**，不会残留进程

> 快捷方式不要直接指向 `elysia_ai.exe`（退出时后端不会跟着关），也不要直接指向 `start_app.bat`（双击 bat 会弹黑框）。调试时可以手动双击 `start_app.bat`，能看到过程输出。

## 开机自启动

在应用「设置」页打开开机自启开关即可，只注册一条启动项（开机后前后端一起启动）；
退出前端时后端同样同步关闭。关闭开关会删除注册项并清理旧的分离式启动项。

## 后端启动（手动方式，调试用）

后端依赖装在项目虚拟环境 `backend\.venv` 里，启动一律用它里面的 python：

```powershell
# 进入后端目录并启动 FastAPI 服务
cd d:\My_Elysia_ai\backend
.\.venv\Scripts\python.exe run.py
```

手动关闭后端（正常关前端会自动关后端，只有手动起了后端才需要）：

```powershell
netstat -ano | findstr :8000
taskkill /PID {上面输出的} /F
```

虚拟环境丢了可以这样重建：

```powershell
cd d:\My_Elysia_ai\backend
D:\Python312\python.exe -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

## 后端测试

```powershell
cd d:\My_Elysia_ai\backend
.\.venv\Scripts\python.exe tests\test_basic.py   # 导入/存储/提示词/工具注册
.\.venv\Scripts\python.exe tests\test_chat.py    # 聊天链路（健康检查/正常/失败补偿）
```


启动成功后访问：
- API 服务：http://127.0.0.1:8000
- 交互式文档：http://127.0.0.1:8000/docs
- 健康检查：http://127.0.0.1:8000/api/health

---

## 前端启动（手动方式，开发调试用）

日常使用走上面的一键启动；只有改代码需要热重载时才这样跑：

```powershell
# 进入前端目录，安装依赖并启动
cd d:\My_Elysia_ai\frontend
flutter pub get
flutter run -d windows
```

> 首次运行 Windows 桌面端需先执行 `flutter create . --platforms=windows` 生成平台文件，
> 并在系统设置中开启**开发人员模式**（`start ms-settings:developers`）。

---

## 快速测试连通性

后端启动后，在另一个 PowerShell 窗口执行：

```powershell
# 健康检查
Invoke-RestMethod http://127.0.0.1:8000/api/health

# 创建对话
$conv = Invoke-RestMethod -Uri http://127.0.0.1:8000/api/conversations -Method POST `
  -ContentType "application/json" -Body '{"title":"测试对话"}'
$conv.conversation_id

# 发送消息（用上面返回的 ID 替换）
Invoke-RestMethod -Uri http://127.0.0.1:8000/api/chat/send -Method POST `
  -ContentType "application/json" `
  -Body "{`"conversation_id`":`"$($conv.conversation_id)`",`"message`":`"你好`"}"
```

---

## 环境要求

| 组件 | 版本 | 路径 |
|------|------|------|
| Python | 3.12.8 | `D:\Python312\python.exe` |
| 后端虚拟环境 | 依赖全在这里 | `backend\.venv\Scripts\python.exe` |
| Flutter SDK | 3.44.2 | `D:\flutter\bin` |
| DeepSeek API Key | 已配置 | `backend\.env` |

---


## 后端日志
下次重启电脑就会自动验证，后端日志在 C:\Users\zack\AppData\Local\MyElysiaAI\backend.log，再出问题直接看它

## 更新依赖

一行命令更新前后端所有依赖：

```powershell
.\update.ps1
```

详细参数说明见 [更新指南.md](更新指南.md)。
