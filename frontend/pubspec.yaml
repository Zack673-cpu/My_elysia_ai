name: demugo_ai
description: "Demugo AI - 心理咨询 AI 助手 (Demugo → Cryene → Elysia)"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  provider: ^6.1.5
  shared_preferences: ^2.5.5
  intl: ^0.20.2
  flutter_markdown_plus: ^1.0.3
  uuid: ^4.5.1
  cupertino_icons: ^1.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
