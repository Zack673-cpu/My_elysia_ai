org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError --enable-native-access=ALL-UNNAMED
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# 关闭 Kotlin 增量编译：修复 C/D 跨盘符路径导致编译崩溃的问题（Windows 已知 bug）
kotlin.incremental=false
# 抑制 JDK 24+ 的 JNI/原生访问警告（红字）
kotlin.daemon.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G --enable-native-access=ALL-UNNAMED
