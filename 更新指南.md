# Demugo AI 更新指南

一键更新前后端所有依赖，不用手动逐个操作。

---

## 快速使用

在项目根目录打开 PowerShell，执行：

```powershell
.\update.ps1
```

脚本会自动完成：
1. **后端**：用 `uv` 将 `requirements.txt` 中所有 Python 包升级到最新兼容版本
2. **前端**：运行 `flutter pub upgrade` 将所有 Flutter 依赖升级到最新兼容版本
3. **验证**：运行 `flutter analyze` 检查代码是否有弃用或错误

---

## 可选参数

| 参数 | 说明 |
|------|------|
| `-UpgradeSDK` | 先升级 Flutter SDK 到最新 stable 版本，再更新依赖 |
| `-FrontendOnly` | 仅更新前端 Flutter 依赖，跳过后端 |
| `-BackendOnly` | 仅更新后端 Python 依赖，跳过前端 |
| `-SkipAnalyze` | 跳过 `flutter analyze` 验证步骤 |

### 示例

```powershell
# 更新全部（后端 + 前端 + analyze）
.\update.ps1

# 全套更新：先升级 Flutter SDK，再更新所有依赖
.\update.ps1 -UpgradeSDK

# 只更新后端
.\update.ps1 -BackendOnly

# 只更新前端，跳过 analyze（速度更快）
.\update.ps1 -FrontendOnly -SkipAnalyze
```

---

## 输出示例

```
>> Updating backend Python deps...
   [OK] Backend dependencies updated

   duckduckgo-search  8.1.1
   fastapi            0.137.1
   openai             2.42.0
   pydantic-settings  2.14.1
   uvicorn            0.49.0

>> Updating frontend Flutter deps...
   [OK] Flutter dependencies updated

>> Running flutter analyze...
   [OK] flutter analyze: no issues

========================================
  Demugo AI - Update Complete!
========================================
  Backend Python deps    OK
  Frontend Flutter deps  OK
```

---

## 关于 "incompatible with dependency constraints"

运行后如果看到类似提示：
```
4 packages have newer versions incompatible with dependency constraints.
```

这些是 **Flutter SDK 内部的传递依赖**（如 `meta`、`matcher`、`vector_math` 等），
被当前 Flutter SDK 版本锁定，`flutter pub upgrade` 无法突破。

解决方法：加 `-UpgradeSDK` 参数升级 Flutter SDK 本身：
```powershell
.\update.ps1 -UpgradeSDK
```

> 升级 SDK 后可能需要更新 `pubspec.yaml` 中的 `sdk:` 约束和部分依赖版本号。

---

## 注意事项

- 脚本使用 `uv pip install --upgrade` 升级后端包，会自动处理依赖冲突
- 如果 `flutter pub upgrade` 失败，脚本会自动降级为 `flutter pub get`（安装当前锁定版本）
- 升级 Flutter SDK 后如遇到弃用警告（如 `Radio.groupValue`、`withOpacity`），需手动修改代码
